# Copy to .env.local at the repo root. See README for migrations, admin seed, and optional features.

# Frontend (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Backend (Express) — same file for local dev
DATABASE_URL=
JWT_SECRET=

# Optional
# PORT=4000
